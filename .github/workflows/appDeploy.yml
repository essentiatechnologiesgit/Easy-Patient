name: CI/CD for STAGE

on:
  push:
    branches:
      - development

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.12.1'  # Adjust version if needed

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'
      
      - name: Install Ruby and Cocoapods
        run: gem install cocoapods

      - name: Install Node.js dependencies
        run: npm install --force

      - name: Install Pods
        run: |
          cd ios
          rm -rf Pods Podfile.lock
          pod install

      # - name: Install Provisioning Profile
      #   run: |
      #     mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
      #     echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode -o ~/Library/MobileDevice/Provisioning\ Profiles/YourProfile.mobileprovision

      # - name: Disable Automatic Signing
      #   run: |
      #     cd ios
      #     xcodebuild -workspace EasyPatientDynamic.xcworkspace -scheme EasyPatientDynamic -configuration Release -sdk iphoneos PROVISIONING_PROFILE_SPECIFIER="" CODE_SIGN_STYLE="Manual"

      # - name: Build Archive with Manual Signing
      #   run: |
      #     xcodebuild clean archive \
      #     -workspace ios/EasyPatientDynamic.xcworkspace \
      #     -scheme EasyPatientDynamic \
      #     -sdk iphoneos \
      #     -configuration Release \
      #     -derivedDataPath ios/build/DerivedData \
      #     -archivePath ios/build/archive/TestingArchive.xcarchive \
      #     PROVISIONING_PROFILE_SPECIFIER="J2N5N2XQ42" \
      #     CODE_SIGN_IDENTITY="Apple Distribution"

      # Optionally re-enable automatic signing after the build
      - name: Enable Automatic Signing
        run: |
          cd ios
          xcodebuild -workspace EasyPatientDynamic.xcworkspace -scheme EasyPatientDynamic -configuration Release -sdk iphoneos CODE_SIGN_STYLE="Automatic"
